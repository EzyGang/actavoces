name: CI

on:
  push:

jobs:
  test:
    name: Test ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Windows
            os: windows-latest
          - name: macOS
            os: macos-latest
          - name: Linux
            os: ubuntu-22.04

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            libasound2-dev \
            libssl-dev \
            pkg-config \
            patchelf

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 10.30.1

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm

      - name: Setup uv
        uses: astral-sh/setup-uv@v8.2.0
        with:
          enable-cache: true

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: "./src-tauri -> target"

      - name: Install frontend dependencies
        run: pnpm install --frozen-lockfile

      - name: Install worker dependencies
        run: pnpm sync:py

      - name: Lint and type-check
        run: pnpm ci-run && pnpm type-check && pnpm lint:rust && pnpm lint:py

      - name: Run frontend tests
        run: pnpm test

      - name: Run Rust tests
        if: runner.os != 'Windows'
        run: pnpm test:rust

      - name: Run Rust tests serially
        if: runner.os == 'Windows'
        shell: pwsh
        run: ./.github/scripts/run-rust-tests-windows.ps1

      - name: Run Python tests
        run: pnpm test:py
